global:
  scrape_interval: 15s # Prometheus akan menarik metrik setiap 15 detik
  evaluation_interval: 15s

scrape_configs:
  # Job untuk memantau Express API Gateway
  - job_name: 'express-gateway'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['express-gateway:3000']